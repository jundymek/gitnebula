---
topic: gitnebula MVP PRD
updated: 2026-08-10T17:37
---

- (decision) Fast path chosen: brief docs/GITNEBULA_PROJECT_BRIEF.md serves as brain dump + product brief; user delegated remaining calls to the facilitator
- (decision) Web research skipped: brief section 2.1 already contains a deliberate competitive landscape (DeepWiki, GitGalaxy, GitDiagram, CodeScene, anvaka) with explicit positioning
- (decision) Stakes calibrated as launch: public OSS release, portfolio piece; visual/repo quality are product requirements per brief section 2
- (assumption) Form factor: CLI + local web app (browser canvas), static bundle export; no mobile/desktop-native surface (brief sections 5, 8)
- (decision) Churn semantics (user-confirmed): churn90d = node's 90d commit count normalized to P95 of same-kind nodes, clamped to [0,1]; hot spot = churn90d >= 0.5, matching mockup HOT_THRESHOLD; resolves brief open question 3 (percentile normalization + absolute threshold on normalized scale)
- (decision) Semantic zoom (user-confirmed): unfold only modules intersecting the viewport (+margin), not all modules at once; deliberate deviation from mockup for performance; user-visible behaviour unchanged
- (decision) DoD demo repos (user-confirmed): fastapi (py), excalidraw (ts), streamlit (mixed py+ts)
- (decision) Brief Q1: TS-only monorepo; TS/JS import parsing via TypeScript compiler API (resolves tsconfig paths, re-exports, index files); Python via tree-sitter-python WASM (web-tree-sitter) to avoid native compilation on user machines under npx
- (decision) Brief Q2: layer detection = ordered rule table over path segments and extensions, overridable per-repo via .gitnebula.yml layers: glob->layer map
- (decision) Brief Q4: gitnebula build emits self-contained bundle: single HTML+JS+CSS with system font stack (no webfonts), analysis.json kept as separate file so CI can refresh data without rebuilding the app; app bundle target < 2 MB gzipped
- (decision) cochanges bounded: only pairs with count >= 3, top 500 per kind; prevents analysis.json blowup on large repos
- (decision) Module metric aggregation: commits90d counted directly per module (commits touching >=1 member file) then P95-normalized among modules; authors = distinct union; lastChangedAt = max; loc = sum
- (decision) Edges: single edges[] with kind=import at both levels; module-level edges aggregated from file-level (weight = count of file import pairs); membership expressed ONLY via node.parent, no member edges in contract (viz derives)
- (decision) cochanges list may contain file-file and module-module pairs (ids resolve to nodes); panel top-3 co-changing modules computed by githist via module pairs, viz only selects/sorts
- (assumption) Module derivation: top-level dirs; when one dir holds >80% of analyzable files (src/ pattern), descend one level, max depth 2; exact rules refined in architecture
- (decision) Offline DoD scoped: local-path analysis 100% offline; URL mode needs network for the shallow clone only
- (decision) No gn binary alias in MVP: collides with Google GN build tool on PATH; revisit post-MVP
- (assumption) Runtime floor: Node >= 20; pnpm workspaces monorepo (architecture to confirm)
- (change) Reviewer gate: reconcile-brief (12 findings) + rubric (verdict ready, 3 medium 6 low); ALL findings accepted and applied in one pass
- (decision) FR-25 + SM-7 added: repo quality (README demo GIF, MIT LICENSE, CONTRIBUTING.md, CI) is a product requirement per brief sections 2/10.6, was silently dropped
- (decision) Hot-spot threshold configurable via .gitnebula.yml, default 0.5 (brief 5.5 said configurable; PRD had hard-coded); ADR-0003 updated
- (decision) Contract field names window-neutral: churn/commits + repo.analysisWindowDays (churn90d would lie under a reconfigured window); breaking-change avoided pre-freeze; ADR-0003 updated
- (decision) Settled operationally defined (displacement-based) in Glossary; constants deferred to architecture as tagged assumption
- (decision) Mockup behaviours adopted as FR consequences: structure-mode hot colour override + legend entry, starfield, selection ring, empty-canvas deselect, drag!=click, persistent search box, hint overlay, fly-to target zooms 2x/3x, reduced-motion fly-to, module count in stats bar
- (decision) One-hop dependency chain declared as deliberate (transitive = v2 candidate); tooltip name+churn declared as deliberate reduction
- (override) Polish step: doc_standards editorial subagents folded into the two-reviewer gate + a facilitator pass (docs written fresh this session; structural+prose findings already covered); deviation from skill defaults logged here
- (event) PRD finalized
