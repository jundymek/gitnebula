---
scope: gitnebula MVP system architecture
purpose: build substrate for parallel terminal-agents implementation
altitude: initiative->epics
updated: 2026-08-10T18:02
---

- (constraint) PRD final (25 FRs) + ADR-0001..0006 are binding inputs: TS-only, TS compiler API + web-tree-sitter WASM, layer rule table, churn P95 formula, bundle shape, contract shape, viewport-scoped unfold
- (constraint) Module boundaries fixed by brief section 9: scanner/deps/githist/viz/cli/describe(reserved); one module = one owner; viz depends on contract+fixtures only
- (constraint) Contract-first ordering: Epic 1 = analysis.json schema+validator+fixtures; everything else depends on Epic 1 at interface level only
- (constraint) terminal-agents execution model: <=5 parallel stories/cohort, one blocking predecessor per story, dot-separated story ids, specs pushed before launch (addendum A6, CLAUDE.md)
- (constraint) Performance budgets: analysis <=60s on 500-2000 files; >=55fps automated floor on 2000-node fixture; viewer bundle <=2MB gz; contract <=5MB
- (constraint) Zero-config npx: no native compilation (node-gyp) anywhere in the install path; Node >=20
- (decision) Monorepo: pnpm workspaces, package = module (user-confirmed after full trade-off discussion vs single src/): per-module manifests kill the shared-package.json merge conflict (job-finder Epic 2 failure mode), viz isolation enforced by build physics not lint, browser/node tsconfig split falls out naturally
- (decision) Viewer chrome: vanilla TS, zero runtime framework (user-confirmed); canvas hand-drawn anyway, ~5 DOM components, tiny own emitter store; runtime deps of viz = d3-force only
- (decision) Deliverables (user-confirmed): BMAD spine in run folder + stable projection at docs/planning-artifacts/architecture.md (path terminal-agents starter prompt reads)
- (decision) Paradigm: pipes-and-filters pipeline (scan -> deps||githist -> assemble+validate -> emit) with a detached consumer (viz) behind the contract artifact; cli is the only orchestrator
- (decision) All cross-module TYPES live in @gitnebula/contract (final analysis.json types + intermediate pipeline types like ScanResult); modules exchange data only as values passed by cli; analyzers never import each other
- (decision) Dependency direction: cli -> {scanner,deps,githist,contract}; scanner/deps/githist -> contract only; viz -> contract only; nothing imports cli; describe = named no-op hook in cli pipeline
- (decision) Render decoupling: viz splits into layout (simulation), render (RenderEngine interface over canvas2d), and chrome (DOM); cosmos.gl swap = new RenderEngine impl only
- (decision) Determinism: analyzers ban Date.now/Math.random (analyzedAt injected by cli); viz force layout seeded from repo.name hash -> deterministic map per repo state (resolves PRD open Q6: yes, seeded)
- (decision) Error policy: per-file errors collected as warnings, never abort; stage failure aborts with 'stage: cause - remedy' shape; drop counters surfaced in terminal summary and repo.stats
- (decision) Config: single resolver in cli; precedence defaults < .gitnebula.yml < CLI flags; resolved config passed to analyzers as plain value (no analyzer reads files/env)
- (decision) Local server: 127.0.0.1 only, serves viz dist + analysis.json; WASM grammars shipped in package, loaded from fs path, never fetched
- (version) Verified current 2026-08: node >=20, typescript 5.x, pnpm 10.x, vite 8.x, vitest 4.x, d3-force 3.x, web-tree-sitter 0.26.x, tree-sitter-python 0.25 (wasm), ajv 8.x, commander (cli), yaml pkg; playwright dev-only for perf harness
- (decision) Schema validation: ajv against JSON Schema draft 2020-12; schema file is the artifact, TS types derived/checked against it in contract package
- (assumption) Fuzzy search implemented in viz (subsequence scorer, no dep) unless story finds fuzzysort necessary
- (question) Exact vitest major (4 vs 5) pinned at first implementation story; both current at authoring
- (change) Reviewer gate: lint 0 findings; rubric needs-work (F1-F8) + reconcile (7 findings); ALL accepted
- (decision) AD-5 reshaped per reconcile F2: swap seam is GraphEngine = layout+render behind ONE interface (load/seed/events/camera/pick/mode/highlight/exportPNG); cosmos.gl owns layout too, so render-only seam was a false escape hatch; MVP impl = d3-force + canvas2d; replacement engines must re-satisfy seeding + Settled semantics; exportPNG re-renders through the same engine path at >=2x
- (decision) Settled constants fixed: max node displacement < 0.5px/frame for 30 consecutive frames, single tunable constant in viz
- (decision) Packaging (rubric F1/F2): pure ESM everywhere, moduleResolution NodeNext; internal packages (contract/scanner/deps/githist) are SOURCE-ONLY (exports -> ./src/index.ts, no build step, no build ordering); cli builds with tsup inlining workspace deps; viz builds with vite to ONE self-contained index.html (inlined JS/CSS) + sibling analysis.json; cli prepack copies viz dist + wasm into cli assets; only cli published; package semver independent of schemaVersion; every package has exports map so deep imports are impossible
- (decision) Data-loading contract (rubric F3): viz fetches ./analysis.json as same-directory sibling in EVERY mode (vite dev aliases a fixture, cli server exposes it, build writes it); viz loader checks schemaVersion major and renders the FR-6 error screen; capability map FR-6 includes viz
- (decision) History & universe semantics (rubric F5 + reconcile F1/F5): ScanResult node set is the closed universe - deps/githist drop+count paths outside it; globs matched ONLY in scanner via picomatch; Config carries windowAnchor (ISO instant, injected by cli, default run start) so analyzers never read the clock; renames via ONE-PASS git log -M --name-status over the window with old->new mapping, NO per-file --follow (60s budget)
- (decision) Fixture repo mechanism (rubric F4): checked-in build script replays crafted commits with pinned GIT_AUTHOR_DATE/GIT_COMMITTER_DATE + fixed identity into gitignored test-fixtures/.generated/, run pretest+CI; deterministic hashes; fixture tests pin windowAnchor
- (decision) AD-10 exceptions (reconcile F3): config schema knows the llm key (cli notice, FR-4) and panel exposes inert description slot (FR-19); everything else stays banned
- (decision) contract package is environment-neutral: no node: imports, no DOM; exports the supported schemaVersion major constant (reconcile F4)
- (decision) AD-9 resolved one-way: JSON Schema is source; TS types GENERATED and committed, CI checks freshness
- (decision) AD-3 signature gains optional onProgress callback now (reconcile F7.1) to avoid mid-story signature break; default-exclude/layer table DATA exported by scanner, RESOLUTION in cli (F7.2)
- (decision) AD-1 clarified: module-level aggregation happens inside deps/githist per ADR-0005; cli assemble only merges and validates
- (version) Stack additions: tsup (cli bundle), picomatch (globs), tree-sitter-cli 0.26.x pinned to web-tree-sitter ABI (grammar built in-repo, .wasm committed, ABI landmine tree-sitter#5171), node:http no framework, open (browser launch), eslint 9 + prettier (AD-4 enforced via no-restricted-globals)
- (decision) Grammar wasm: built in-repo by pinned tree-sitter-cli, committed as deps asset, resolved via import.meta.url so the same path works source-mode and bundled
- (event) spine finalized
