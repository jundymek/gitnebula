# Story 6.4: Every control stays reachable at a real window size

> **Status: ready-for-dev** · Owner: `viz` · Touches: — · Depends_on:
> `[6.1-viz-ui-suite]` · **Wave B** — launched from
> `epic/6-assembled-viewer` with wave A already in the base.
>
> **Read this before configuring anything.** 6.1 is listed as a dependency
> because the `ui` suite is a **fact about your base branch**, not a running
> peer you wait on: wave A has already merged before your launch. Do **not**
> run `set-depends-on.sh` — there is no agent to wait for. Configure the wave B
> cohort instead:
> `~/.terminal-agents/scripts/set-cohort.sh <agents-of-6.2,6.3>`, declare your
> territory in `intent.md`, and complete intent-sync.
>
> **Territory (yours):** new spec files under `packages/viz/ui/tests/` for
> layout reachability. You do NOT touch `packages/viz/src/` — not the
> stylesheet, not `tooltip.ts`, not the panel. If a real reachability defect
> turns up, **report it**; fixing it is a change to shipped behaviour and needs
> its own story, and 6.5 already owns the chrome in this epic.
>
> **New files only.** Your peers 6.2 and 6.3 are writing specs into the same
> directory in the same wave. A new file is a merge neither branch can lose.
>
> **PR base = `epic/6-assembled-viewer`**, not `master`.

## Story

As a reader on a short screen looking at a file with many co-change partners,
I want the panel's actions to stay reachable,
So that content past the fold scrolls into view instead of being lost.

## Context (binding)

- `epics.md` Epic 6; FR-27 (blast radius), FR-26 (start-here), UX-DR14 (empty
  and bounded states name their cause).
- **Why this story exists.** The guard that exists today is a **regex over the
  stylesheet**. `packages/viz/src/chrome/blast-radius.test.ts:412-431` reads
  `styles.css` as text and asserts that `max-height:` and `overflow-y: auto`
  appear in the `.p-blast-list` and `#panel` rules. Its own comment calls this
  "a weaker check than a rendered one, and still enough to catch the cap being
  deleted". It would pass with `max-height: 0`, it would pass if a later rule
  overrode the cap, and it would pass if a flex parent broke it.
- **The defect it guards is real and was measured.** `styles.css:874-881`
  records it: `body` is `overflow: hidden`, so anything past the fold is
  **unreachable rather than merely off-screen**; one real file on this
  repository has **44 co-change partners**, and at ~23 px a row that is over
  1,000 px of list, which pushed the `show on map` toggle and the panel's own
  actions below the viewport where they could not be reached at all.
- **Three regions depend on real viewport height**, and jsdom computes no
  layout for any of them:
  - `#panel` — `max-height: calc(100% - 36px); overflow-y: auto`
    (`styles.css:389-390`)
  - `#start-here` — the same bound (`styles.css:529-530`)
  - `.p-blast-list` — `max-height: min(38vh, 320px)` (`styles.css:884-885`)
- **No source reads `scrollTop`, `scrollHeight` or `scrollIntoView`** anywhere
  in `packages/viz/src/`. So there is no scroll *behaviour* to test — the thing
  to verify is **reachability**: that a control is inside the viewport and
  clickable. Do not invent scroll-position assertions; assert that the reader
  can get to the control.
- **The tooltip's flip has never run against a measured box.**
  `packages/viz/src/chrome/tooltip.ts:104-107` measures `element.offsetWidth`
  and `offsetHeight`, and in jsdom both are always **0**. The pure function
  `tooltipPosition` is already tested exhaustively, including a grid sweep of
  the viewport asserting the never-overflow invariant
  (`chrome/tooltip.test.ts:46-86`) — **do not re-test the function.** What is
  untested is the chain "real font renders → real text width → correct flip",
  because its inputs have never been real. Note that
  `tooltip.test.ts:116-122` passes only because the box is 0×0.
- **The tooltip carries `aria-hidden="true"`** (`tooltip.ts:89`), so it is
  invisible to role and accessible-name queries by construction. Select it by
  class or by its element, and assert its **rendered rectangle**.
- **This closes an item Epic 5 left open.** `docs/dev/epic-5/5.6-viz-blast-radius/MANUAL_TESTING.md:96`
  carries "Scrolling felt in a browser" unticked, because the agent had no
  browser and no eyes. The mechanism is automatable; whether it *feels* right
  is not, and the two must not be conflated.
- **Fixture.** `root-files` is small but has only one co-change pair, so it
  cannot produce a long list. To exercise the 44-partner case you need a
  document with a long partner list — build one for your specs from a valid
  base, and do **not** add it to `packages/contract/fixtures/`. State in the
  README how you produced it, so the measurement is reproducible.
- **Determinism (AD-6).** Await `settled` before measuring anything
  positional — never a timeout.

## Acceptance Criteria

- AC-1: With the panel open on the node carrying the longest co-change list,
  at a viewport of 1280×800 **and** again at a height of about 600 px, the
  `show on map` control and the panel's own actions are inside the viewport
  and clickable. The check is a **rendered** one — element boxes measured in
  the browser — not a match against stylesheet text.
- AC-2: Each of the three bounded regions — `#panel`, `#start-here`,
  `.p-blast-list` — scrolls its own content when filled past its cap, and none
  of them pushes a control outside the window.
- AC-3: The reachability assertion is shown able to fail, using the negative
  control pattern story 6.1 established: with the height cap defeated, the test
  goes red. A check that cannot fail is not a check.
- AC-4: A node with a long path is hovered near the right edge and near the
  bottom edge, and the tooltip's **rendered rectangle** stays inside the
  viewport — the flip proven against a measured box rather than a 0×0 one.
- AC-5: `packages/viz/src/chrome/tooltip.ts` and `tooltip.test.ts` are
  unchanged: this story tests the function's real inputs, not its logic, which
  is already covered.
- AC-6: The story's README records, for each region, the measurement that
  makes the case concrete — the list length used, the viewport heights, and
  whether any control was found unreachable. If one is, it is **reported, not
  fixed**: changing shipped layout needs its own story.
- AC-7: No file under `packages/viz/src/` is modified, no fixture is added to
  `packages/contract/fixtures/`, no dependency is added, and every new spec is
  a **new file** under `packages/viz/ui/tests/`.
- AC-8 (human-review): The "scrolling felt in a browser" item that story 5.6
  left unticked is revisited — either ticked with the evidence named, or
  restated in `MANUAL_TESTING.md` as the part that remains genuinely
  subjective now that the mechanism is covered.

## Tasks / Subtasks

- [x] Build a document with a long co-change partner list; record how (AC-1)
- [x] Reachability spec at 1280×800 and at ~600 px height (AC-1)
- [x] Bounded-region spec for `#panel`, `#start-here`, `.p-blast-list` (AC-2)
- [x] Negative control: defeat the cap, watch the test go red (AC-3)
- [x] Tooltip edge-flip against a measured box, right edge and bottom edge
      (AC-4)
- [x] Per-story docs: `docs/dev/epic-6/6.4-viz-reachability/README.md` with
      the measurements (AC-6)
- [x] `MANUAL_TESTING.md`: revisit 5.6's unticked scrolling item, and leave
      AC-8 for the owner

## Testing

Test command: `pnpm --filter @gitnebula/viz test`

Your own suite runs with `pnpm --filter @gitnebula/viz ui`. Quote both in the
PR body.

Note the canonical table in `CLAUDE.md`: the cli package is `gitnebula`,
unscoped. `pnpm --filter @gitnebula/cli test` matches no project — since story
5.9 that exits **1** rather than silently passing, but do not write it.

## PR checklist (harness gates)

- PR base = `epic/6-assembled-viewer`; title e.g.
  `test(viz): prove the panel's controls stay reachable at a real window size`.
- PR body: `## Manual verification` + trailers `Refs: 6.4-viz-reachability`,
  `Agent: <your-agent-name> (terminal-agents, <model-id>)`.
- Docs artifact present: `docs/dev/epic-6/6.4-viz-reachability/README.md`.
- One line per paragraph in the PR body — GitHub renders each newline as a
  break.

## Dev Agent Record

### Agent Model Used

`claude-opus-5` (terminal-agents, agent `pamela`).

### Debug Log References

Four things went wrong first, and each was a real correction rather than a typo.

1. **The partner-row count is 45, not 44.** `root-files` already pairs
   `fp/proxy.py` with `test_proxy.py`, and the builder *adds to* the base
   document rather than replacing its co-change list. The first run failed on
   `Expected: 44 / Received: 45`. `EXPECTED_PARTNER_ROWS` now carries the
   reason, and `buildLongCochangeDocument` re-derives the count from the
   document it just built and throws if the two disagree.

2. **`#start-here` was already open.** `chrome.ts:241-247` opens it on the first
   settle when nothing is selected, and the engine's `settled` reaches chrome
   synchronously — before the `setSelected` that runs a microtask later. So
   clicking the header button unconditionally *shut* it. The spec now reads
   `aria-expanded` first, which is what the attribute is for.

3. **`scrollIntoViewIfNeeded` scrolls the document even when `body` is
   `overflow: hidden`.** AC-3's negative control caught it: with the caps
   removed, every control was still reported reachable, because the helper was
   rescuing the page in a way no mouse can — `overflow: hidden` stops a reader,
   not a script. The spec now finds the nearest genuine scroll container
   (computed `overflow-y` of `auto`/`scroll` **and** actually overflowing) and
   scrolls only that, by the minimum needed. This is the most important
   correction in the story: without it the new check had exactly the blind spot
   it was written to remove.

4. **The wheel test failed twice, for two different browser facts.** A wheel is
   consumed by the *innermost* scroll container under the pointer, so a gesture
   at the panel's centre scrolls `.p-blast-list` and leaves `#panel` at zero;
   and Chromium applies wheel scrolling asynchronously, so `scrollTop` read on
   the next line is stale. Both are recorded in the README.

Assertions watched failing, deliberately, after they were green: with
`.tooltip { font-size: 0 }` injected so the box measures 0×0 — the jsdom
condition — the "real box" check goes red (`measured a 0x0 box`) and the
right-edge check goes red on its own premise guard (`would have ended at 1254px
in a 1280px window, which does not overflow`). Reverted; the suite is green as
committed.

### Completion Notes List

- **AC-1 met, and the case reproduced.** At 1280×600 both `show on map`
  (635 → 665) and `isolate` (679 → 709) sit **below the fold** of a 600 px
  window before anything is scrolled, and are reachable only by scrolling
  `#panel`. At 1280×800 they are already in view. Both are hit-tested with
  `elementFromPoint` and then actually clicked, with the engine required to
  agree (`getBlastRadius()` = 45 ids, `getIsolated()` = `fp/proxy.py`).
- **AC-2 met.** All three regions overflow at 1280×600, compute
  `overflow-y: auto`, scroll, and keep their own boxes inside the window. A
  second test drives a real 400 px `mouse.wheel` over the list and over the
  panel, and asserts the page itself never moves.
- **AC-3 met.** The caps are defeated in-page with `page.addStyleTag` and the
  same reachability function is required to fail. The test guards both ends: it
  asserts the controls *were* reachable with the caps intact (a check that is
  red either way proves nothing), and that the override actually took effect
  (panel 1502 px in a 600 px window, `overflow-y: visible`).
- **AC-4 met.** The tooltip renders a **643.125 × 26 px** box — 0 × 0 in jsdom
  — and at the right edge it flips to the far side of the cursor, landing
  strictly left of where a clamp would put it. Both edges keep the rendered
  rectangle inside the window.
- **AC-5 met.** `tooltip.ts` and `tooltip.test.ts` are byte-identical to the
  base branch. `tooltipPosition` is not re-tested; only its real inputs are.
- **AC-6 met.** The README records row height (25 px), list content (1125 px),
  and per-region content/visible heights at both window sizes, and answers the
  "was any control found unreachable" question directly: **yes, both, at
  1280×600, before scrolling.**
- **AC-7 met.** `git status` shows the new paths and the two doc updates and
  nothing else. No file under `packages/viz/src/` is modified; no fixture was
  added to `packages/contract/fixtures/`; no dependency was added; both specs
  are new files under `packages/viz/ui/tests/`.
- **AC-8 addressed and left for the owner.** Story 5.6's shipped
  `MANUAL_TESTING.md` is **not** edited — it is a frozen record of completed
  work. The revisit is in this story's `MANUAL_TESTING.md` §7, quoting the
  original item and splitting it: the mechanism is now covered (ticked, with
  the evidence named); the *feel* is left unticked and addressed to the
  maintainer as the only genuinely subjective part remaining.

**Finding, reported and not fixed: the tooltip is positioned in the wrong
coordinate space.** `.tooltip` is `position: fixed` (`styles.css:315`), so the
`left`/`top` written at `tooltip.ts:117-118` are viewport coordinates — but the
`screen` point the hover event carries is canvas-relative
(`engine.ts:1789-1791` subtracts the canvas' `rect.top`), and the canvas sits
below a 103 px header. Measured at 1280×800 the tooltip renders **89 px above
the cursor** (= 103 − 14, the header less the intended offset), and the
bottom-edge flip is **unreachable by any real pointer**: it fires above a canvas
y of 752, and the canvas is only 697 px tall. That branch is exercised today
only by the unit test calling the pure function directly. The error is upward,
into the page, so it never overflows the window and AC-4's stated requirement
holds. Fixing it means editing `packages/viz/src/`, which AC-5 and AC-7 forbid
this story. The specs assert **nothing** about the offset in either direction,
so every assertion here survives a future fix.

**Also reported, not fixed: `pnpm lint` fails on `intent.md`.**
`.prettierignore` exempts `plan.md`, `DECISIONS.md` and `PR_SUMMARY.md` as
worktree scratch files, but not `intent.md`, a newer convention. The file is
gitignored and never reaches a commit, so the failure is spurious and the local
workaround is one Prettier run. It is one line in a shared file that all three
wave-B agents hit in the same hour, so it is reported once rather than patched
from three branches. Both peers were told directly.

**`sprint-status.yaml` is deliberately untouched**, on superman's instruction of
2026-09-08T15:36:24Z making the supervisor its single writer at closure. That
contradicts this project's own "flip only your own story's row" rule; superman
has logged the contradiction for the retrospective, and this story follows the
supervisor. The `6.4-viz-reachability` row therefore still reads `backlog`.

### File List

**NEW**

- `packages/viz/ui/tests/reachability.pw.ts` — AC-1, AC-2, AC-3: the panel's
  controls, the three bounded regions, the wheel, and the negative control.
- `packages/viz/ui/tests/tooltip-edges.pw.ts` — AC-4: the flip against a
  measured box, at the right and bottom edges.
- `packages/viz/ui/tests/support/long-cochange-document.ts` — builds the
  45-partner document from the `root-files` fixture, validates it against the
  contract schema, and serves it per-page over `page.route`.
- `docs/dev/epic-6/6.4-viz-reachability/README.md` — the measurements (AC-6)
  and the two findings.
- `docs/dev/epic-6/6.4-viz-reachability/MANUAL_TESTING.md` — the steps, marked
  with what was actually run, plus AC-8's revisit.

**UPDATE**

- `docs/implementation-artifacts/epic-6-assembled-viewer/6.4-viz-reachability.md`
  — this file: tasks ticked, Dev Agent Record filled.

**UNCHANGED, and verified so** — every file under `packages/viz/src/`,
`packages/viz/ui/playwright.config.ts`,
`packages/viz/ui/src/suite-conventions.test.ts`,
`packages/viz/ui/tests/smoke.pw.ts`, `packages/viz/harness/page-helpers.ts`,
`packages/contract/fixtures/`,
`docs/implementation-artifacts/sprint-status.yaml`, and
`docs/dev/epic-5/5.6-viz-blast-radius/MANUAL_TESTING.md`.
