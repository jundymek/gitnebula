# Story 1.2: Contract schema and validator

> **Status: ready-for-dev** · Owner: `contract` · Touches: — · Depends_on:
> `[1.1-workspace-scaffold]`
>
> **This task depends on story 1.1 — you need its code merged into your
> base.** Translate the story number to the agent name by launch order, then
> immediately run:
> `~/.terminal-agents/scripts/set-depends-on.sh <agent-of-1.1>:done`
> While waiting you may draft the schema in your plan, but write no code
> until the scaffold lands and you have rebased.

## Story

As a pipeline or viewer developer,
I want the frozen `analysis.json` JSON Schema with generated types and a validator,
So that every module builds against one machine-checked truth.

## Context (binding)

- PRD FR-6/FR-7 (contract content), ADR-0003 (churn fields), ADR-0005
  (contract shape: parent-only membership, two-level edges, bounded
  cochanges, stable sorts), AD-1 (intermediate types live here), AD-9
  (schema is source, types generated), AD-13 (`windowAnchor` in `Config`).
- The schema IS the project's central artifact (brief §7). Changing it later
  is a versioned act — get it right against the PRD Glossary, not against
  your intuition.

## Acceptance Criteria

- AC-1: JSON Schema (draft 2020-12) in `@gitnebula/contract` describes:
  `schemaVersion` ("1.0"), `repo` (name, remoteUrl nullable, analyzedAt,
  defaultBranch, `analysisWindowDays`, stats: files/loc/commits/languages),
  `nodes[]` (`id`, `kind` module|file, `parent` nullable, `path`, `layer`
  backend|frontend|infra|test|other, `loc`, `churn` 0..1, `commits` int,
  `authors`, `lastChangedAt`, `description: null`, `descriptionSource:
  null` — both nullable, required-present), `edges[]` (`source`, `target`,
  `kind: "import"`, `weight`), `cochanges[]` (`a`, `b`, `count`).
- AC-2: Type generation script produces committed TS types from the schema;
  a CI check fails when types are stale (AD-9); no hand-edited type drift is
  possible.
- AC-3: Package exports `validateAnalysis(data)` (ajv, compiled once) with
  error output usable in cli failure messages, and `SUPPORTED_SCHEMA_MAJOR`.
- AC-4: Intermediate pipeline types exported: `ScanResult`, `DepsResult`,
  `GitResult`, `Config` (including `windowAnchor: string` ISO, excludes,
  windowDays, layers overrides, hotspotThreshold, llm passthrough field).
- AC-5: Package remains environment-neutral: no `node:` imports anywhere in
  its runtime path (viz will import it in the browser); verified by the viz
  placeholder build still passing.
- AC-6: Unit tests: a valid minimal document passes; each required field's
  absence fails with a pointer to the field; an unknown `layer` value fails.

## Tasks / Subtasks

- [x] Author `analysis.schema.json` per AC-1 (PRD Glossary names, exactly)
- [x] Type-generation script (`pnpm --filter @gitnebula/contract generate`) + committed output
- [x] CI freshness check (regenerate + `git diff --exit-code`) added to ci.yml
- [x] `validateAnalysis` + `SUPPORTED_SCHEMA_MAJOR` exports
- [x] Intermediate types (`ScanResult`, `DepsResult`, `GitResult`, `Config`)
- [x] Unit tests per AC-6
- [x] Per-story docs: `docs/dev/epic-1/1.2-contract-schema/README.md`
      (schema-change protocol: version bump discipline, regeneration)
- [x] Manual testing: not applicable — state why in plan.md

## Testing

Test command: `pnpm --filter @gitnebula/contract test`

## PR checklist (harness gates)

- PR base = the epic branch; title e.g. `feat(contract): analysis.json schema, generated types, validator`.
- PR body: `## Manual verification` + trailers `Refs: 1.2-contract-schema`,
  `Agent: <your-agent-name> (terminal-agents, <model-id>)`.

## Dev Agent Record

### Agent Model Used

`claude-opus-5` (terminal-agents agent `bob`).

### Debug Log References

- `pnpm --filter @gitnebula/contract test` — 81 passed (1 file).
- `pnpm lint` (ESLint + Prettier), `pnpm typecheck` (6 packages), `pnpm test`
  (7 suites) — all clean.
- `pnpm build` — tsup (cli) and vite (viz) both succeed with the contract
  imported into the browser bundle: AC-5's env-neutrality check.
- CI freshness step reproduced locally: `generate` then
  `git diff --exit-code -- packages/contract/src/generated` → no drift.
- Mutation checks, to prove the suite is not vacuous: (a) dropping the
  missing-property pointer suffix in `validate.ts` fails 35 of 47 tests;
  (b) adding `"database"` to the `layer` enum fails the unknown-layer test.
  On the date-time validator: (c) forcing `isLeapYear` false fails 1 test,
  (d) removing the day-of-month bound fails 6, (e) removing the hour/minute
  bound fails 2, (f) accepting `:60` again fails 3, (g) dropping the offset
  bound fails 2. All reverted; suite green afterwards.
- `codex exec review --base epic/1-workspace-scaffold` — run three times. Each
  pass raised one P2 on the `date-time` format and each was fixed; the third
  pass drove the design to the Date-parseability invariant. See the note below.
- `node -e` check confirming `new Date('...T23:59:60Z')` is `Invalid Date`, which
  is what settled the leap-second question.

**Harness defect — `DOCS_TASK_DIR` is not propagated from the project config
into `.env.agent` (not fixed; worked around).** The pre-PR docs gate blocked
`gh pr create` with `Expected: docs/backend/<epic>/1.2-contract-schema/README.md`,
even though this project's config declares the correct value:

```
$ grep DOCS_TASK_DIR ~/.terminal-agents/config/projects/gitnebula/project.env
DOCS_TASK_DIR='docs/dev'
$ grep DOCS_TASK_DIR ~/.local/state/terminal-agents/bob/.env.agent
DOCS_TASK_DIR=docs/backend        # <- wrong; the project value never arrived
$ grep DOCS_TASK_DIR ~/.local/state/terminal-agents/alice/.env.agent
DOCS_TASK_DIR=docs/backend        # <- same for story 1.1, so it is not per-agent
```

`spawn-agent.sh` writes the key unconditionally but with the default rather than
the project's value, so **every agent in this project inherits `docs/backend`**
and the gate demands a path CLAUDE.md forbids. Not fixed here: it is one line in
the harness, outside this story, and four agents patching it four ways is worse
than the defect.

Worked around by correcting the stale value in this agent's own
`~/.local/state/terminal-agents/bob/.env.agent` (runtime state, not the
installed harness) to the value the project already declares. **Cost:** one
blocked `gh pr create`, plus this investigation. The alternative — committing a
duplicate README under `docs/backend/` — was rejected: it would put a file in
the repo at a path the project rules reject, permanently, to satisfy a
misconfigured gate. The fix belongs in `spawn-agent.sh`; until it lands, every
agent on this project will hit this at PR time.

### Completion Notes List

- Schema is draft 2020-12, `additionalProperties: false` at every level, with
  `$defs` for the reused shapes (`layer`, `nodeKind`, `repo`, `repoStats`,
  `node`, `edge`, `cochange`). `$id` pins the version:
  `…/schema/analysis-1.0.json`.
- Types are generated by `scripts/generate-types.mjs`
  (`json-schema-to-typescript`) into `src/generated/analysis.ts`, committed,
  do-not-edit banner, ESLint-ignored, CI-verified fresh. The generator script
  is the only place in the package allowed `node:` imports.
- `validateAnalysis` compiles the schema once at module load (both the cli and
  the Viewer validate on a hot path) and returns a discriminated result that
  narrows to `AnalysisDocument` on success. Missing-property errors are
  rewritten to point at the absent field, not its parent, so the pointer is
  printable verbatim in an AD-7 cli message.
- `date-time` is registered as a local RFC 3339 validator rather than adding
  `ajv-formats`; it is the only format the contract uses. It validates
  component ranges and the calendar, not just digit placement. This took three
  Codex passes, each one right: the original was a shape-only regex accepting
  `2026-99-99T25:61:61Z`; the second version allowed `:60` at any time of day;
  the third pass observed that `new Date("…T23:59:60Z")` is `Invalid Date`, so
  admitting *any* leap second preserved the very NaN-in-the-panel failure the
  range checks were added to prevent. Settled on an invariant instead of a
  rule list — **every accepted instant must parse with `new Date(...)`**, which
  a test asserts over the whole valid table — so `:60` is rejected, deliberately
  narrower than RFC 3339 §5.6 (see PR body). 34 test cases; the fix also
  deleted the offset arithmetic the leap-second case had needed.
- Intermediate types in `src/pipeline.ts` derive from the generated contract
  types via `Pick`/`Omit`, so a schema change breaks the analyzers at compile
  time instead of drifting. Adds `AnalyzerWarning` for AD-7's counted
  non-fatal drops.
- Five decisions are carried in the PR body because later stories depend on
  them: `lastChangedAt` nullable, `description`/`descriptionSource` typed
  `null`-only, co-change `count` bound `>= 1` in the schema (`>= 3` stays
  analyzer policy per FR-7), `repo.stats.languages` as a share map, and the
  deliberate leap-second deviation above.
- Per-story docs follow the spec path (`docs/dev/epic-1/…`), not the generic
  pre-PR checklist's `docs/features/epic-1-omdb-data-layer/…`, which belongs to
  a different project. Divergence flagged.
- Not done, deliberately: no fixture `analysis.json` files (story 1.3), no
  widening of `description` to `string | null` (the describe-layer story's
  act), no CI trigger change (maintainer decision, revisited in story 4.2).

### File List

- `packages/contract/src/analysis.schema.json` (NEW)
- `packages/contract/src/validate.ts` (NEW)
- `packages/contract/src/pipeline.ts` (NEW)
- `packages/contract/src/generated/analysis.ts` (NEW, generated)
- `packages/contract/scripts/generate-types.mjs` (NEW)
- `packages/contract/src/index.ts` (MODIFIED)
- `packages/contract/src/index.test.ts` (MODIFIED)
- `packages/contract/package.json` (MODIFIED — ajv, json-schema-to-typescript, `generate`)
- `packages/contract/tsconfig.json` (MODIFIED — `resolveJsonModule`)
- `.github/workflows/ci.yml` (MODIFIED — type-freshness step)
- `eslint.config.js` (MODIFIED — ignore generated, allow Node globals in `packages/*/scripts`)
- `pnpm-lock.yaml` (MODIFIED)
- `docs/dev/epic-1/1.2-contract-schema/README.md` (NEW)
- `docs/implementation-artifacts/sprint-status.yaml` (MODIFIED — status row)
