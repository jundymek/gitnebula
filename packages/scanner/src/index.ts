// @gitnebula/scanner — file tree, LOC, language and layer detection.
// Story 2.1 implements the analyzer; this stub only proves the AD-2 edge.

import { packageName as contractPackageName } from "@gitnebula/contract";

export const packageName = "@gitnebula/scanner";
export const contractEdge = contractPackageName;
