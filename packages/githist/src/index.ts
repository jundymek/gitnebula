// @gitnebula/githist — churn, authors and co-change from `git log`.
// Story 2.3 implements the analyzer; this stub only proves the AD-2 edge.

import { packageName as contractPackageName } from "@gitnebula/contract";

export const packageName = "@gitnebula/githist";
export const contractEdge = contractPackageName;
