// @gitnebula/contract — the analysis.json schema and shared pipeline types.
// Story 1.2 replaces this stub with the JSON Schema and generated types (AD-9).
// This package is environment-neutral: no `node:` imports, no DOM (AD-11) —
// both the Node pipeline and the browser bundle consume it.

export const packageName = "@gitnebula/contract";
