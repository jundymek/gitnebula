import { defineConfig } from "vite";

// Placeholder build config. Story 4.1 turns this into the single
// self-contained index.html bundle (AD-11); until then a default build
// proves the vite build edge exists and runs.
export default defineConfig({});
